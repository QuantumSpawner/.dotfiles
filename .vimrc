" source default vimrc (see `:help defaults.vim`)
unlet! skip_defaults_vim
source $VIMRUNTIME/defaults.vim

" display
set number
set relativenumber

" search
set hlsearch
set ignorecase
set smartcase

" system
set clipboard=unnamedplus
set mouse=a

